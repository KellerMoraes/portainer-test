import { defineStore } from 'pinia';
import { ref } from 'vue';
export const useResponsividadeStore = defineStore('responsividade', { 
  state: () => ({
    state: () => ({
        // Responsividade
        tamanhoColunagemResponsiva: ref(null)
    
      }),
  

  }),
})